# target-tracking-service가 실제로 떠 있는 주소.
# 기본값(코드 상 fallback)은 Tailscale Funnel로 공개된 클러스터 주소이므로,
# 이 값을 굳이 안 채워도 그 주소로 동작한다. 로컬 백엔드나 다른 배포를 향하게
# 하려면 여기서 오버라이드.
NEXT_PUBLIC_BACKEND_URL=https://k3s-master.taildcdcee.ts.net
